apiVersion: v1
kind: Service
metadata:
  name: elastickube-api
  namespace: kube-system
  labels:
    name: "elastickube-api"
spec:
  selector:
    k8s-app: elastickube-api
    version: v1
  ports:
  - name: http
    port: 80
    protocol: TCP
